# Mark Lombok-generated methods with @lombok.Generated so JaCoCo (0.8+) excludes
# getters/setters/equals/hashCode/builders from coverage — coverage then reflects
# hand-written logic, not boilerplate.
config.stopBubbling = true
lombok.addLombokGeneratedAnnotation = true
